// Array que recorre mediante un metodo length cuantos elementos hay dentro del array

var prompt = require('prompt-sync')()

var amigos = ['Carlos', 'Juan', 'Pedro']
console.log('Tenés ' + amigos.length + ' amigos dentro del array')